import { CurrentPricePipe } from './current-price.pipe';

describe('CurrentPricePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentPricePipe();
    expect(pipe).toBeTruthy();
  });
});
